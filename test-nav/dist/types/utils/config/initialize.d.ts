import { BalConfig, BalConfigState } from './config.types';
export declare const defaultConfig: BalConfigState;
export declare const defaultLocale: string;
export declare const initialize: (userConfig?: BalConfig, win?: any) => void;
export default initialize;
